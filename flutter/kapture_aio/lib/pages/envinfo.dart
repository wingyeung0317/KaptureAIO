import 'package:flutter/material.dart';

class EnvInfo extends Container {
  EnvInfo({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello World'),
    );
  }
}