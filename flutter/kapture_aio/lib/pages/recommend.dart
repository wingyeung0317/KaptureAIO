import 'package:flutter/material.dart';

class Recommend extends Container {
  Recommend({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello World'),
    );
  }
}